public class CountryQueue implements Queueable {
    public void execute(QueueableContext context) {
       Account a = new Account(Name='CountryQueue',Phone='(415) 555-1212');
       insert a; 
    }	
}